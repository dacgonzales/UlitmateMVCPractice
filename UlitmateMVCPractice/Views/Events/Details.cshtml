@model UlitmateMVCPractice.Models.Event

@{
    
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>
    <div class="row">
        <div class="col-md-8">
            <h1>WHAT: <span class="text-primary">@Model.Eventname</span></h1>
            <h3>WHEN: <span class="text-primary">@Model.EventDate</span></h3>
            <h3>WHERE: <span class="text-primary">@Model.EventVenue</span></h3>
        </div>
        
        <div class="col-md-4">
            @{Html.RenderAction("Create", "Comments", new { id = Model.EventId });}
        </div>

    </div>
          
    <hr/>
            <p>                
                @Html.ActionLink("Back to Event List", "Index")
            </p>
</body>

</html>
